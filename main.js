var numero1 = Number(prompt("Digite primeiro numero"))

function somar() {
    let prox = prompt("Digite proximo numero")
    numero1 += Number(prox)
    alert(numero1)
    console.log (numero1)
}

function subtrair() {
    let prox = prompt("Digite proximo numero")
    numero1 -= Number(prox)
    alert (numero1)
    console.log (numero1)
}

function dividir() {
    let prox = prompt("Digite proximo numero")
    numero1 /= Number(prox)
    alert (numero1)
    console.log (numero1)
}

function multiplicar() {
    let prox = prompt("Digite proximo numero")
    numero1 *= Number(prox)
    alert (numero1)
    console.log (numero1)
}

function modulo(){    
let prox = prompt("Digite proximo numero")
    numero1 %= Number(prox)
    alert (numero1)
    console.log (numero1)
}

function zerar(){
    numero1 = Number(0)
}

